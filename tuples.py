coordinates = (5, 10) # creating a tuple
print(coordinates) 
print(coordinates[0]) # accessing elements of a tuple
print(coordinates[1]) # tuples are immutable (cannot be changed)
# coordinates[0] = 15 # this will raise an error
